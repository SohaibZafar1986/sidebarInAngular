import { SidebarNavCheckboxPipe } from './sidebar-nav-checkbox.pipe';

describe('SidebarNavCheckboxPipe', () => {
  it('create an instance', () => {
    const pipe = new SidebarNavCheckboxPipe();
    expect(pipe).toBeTruthy();
  });
});
